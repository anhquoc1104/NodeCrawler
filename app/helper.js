//require dependences
const _ = require('lodash');
const axios = require('axios');
const cheerio = require('cheerio');

//Handles the request Promise when it is fulfilled
//and sends a JSON response to the HTTP response streams res
const
